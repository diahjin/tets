import React,{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';import{Auth}from'./Login';
export default function Register(){const{register}=useAuth(),nav=useNavigate();const[f,setF]=useState({email:'',username:'',password:''}),[err,setErr]=useState('');async function submit(e){e.preventDefault();try{await register(f);nav('/')}catch(x){setErr(x.message)}}return <Auth title="Registro"><form onSubmit={submit}><input placeholder="Email" onChange={e=>setF({...f,email:e.target.value})}/><input placeholder="Usuario" onChange={e=>setF({...f,username:e.target.value})}/><input type="password" placeholder="Contraseña mínimo 8 caracteres" onChange={e=>setF({...f,password:e.target.value})}/>{err&&<p className="error">{err}</p>}<button>Crear cuenta</button><Link to="/login">Ya tengo cuenta</Link></form></Auth>}

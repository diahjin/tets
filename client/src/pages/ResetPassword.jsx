import React,{useState}from'react';import{Auth}from'./Login';import{api}from'../services/api';
export default function ResetPassword(){const token=new URLSearchParams(location.search).get('token');const[password,setPassword]=useState(''),[msg,setMsg]=useState('');async function submit(e){e.preventDefault();const r=await api('/auth/reset-password',{method:'POST',body:{token,password}});setMsg(r.message)}return <Auth title="Nueva contraseña"><form onSubmit={submit}><input type="password" placeholder="Nueva contraseña" value={password} onChange={e=>setPassword(e.target.value)}/><button>Actualizar</button>{msg&&<p className="ok">{msg}</p>}</form></Auth>}

import React,{useEffect,useState}from'react';import{api}from'../services/api';import{PieChart,Pie,Tooltip,ResponsiveContainer,Cell,BarChart,Bar,XAxis,YAxis}from'recharts';
export default function Dashboard(){const[stats,setStats]=useState(null);useEffect(()=>{api('/stats').then(setStats)},[]);if(!stats)return <p>Cargando...</p>;const pie=[{name:'Victorias',value:stats.totals.wins},{name:'Empates',value:stats.totals.draws},{name:'Derrotas',value:stats.totals.losses}];return <div><h2>Dashboard</h2><div className="grid"><Card title="Total combates" value={stats.totals.total}/><Card title="% victorias" value={`${stats.totals.winRate}%`}/><Card title="Más usado" value={stats.mostUsed?.name||'Sin datos'}/><Card title="Mejor winrate" value={stats.bestWinRate?.name||'Sin datos'}/></div><section className="panel"><h3>Resultados</h3><div className="chart"><ResponsiveContainer><PieChart><Pie data={pie} dataKey="value" nameKey="name" outerRadius={90}>{pie.map((_,i)=><Cell key={i}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div></section><section className="panel"><h3>Ranking de Bleys</h3><div className="chart"><ResponsiveContainer><BarChart data={stats.ranking.slice(0,8)}><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="score"/></BarChart></ResponsiveContainer></div></section><section className="panel"><h3>Últimos combates</h3>{stats.latestCombats.map(c=><p key={c.id} className="row"><b>{c.bleyName}</b> {c.result} vs {c.rival||'rival'} · {new Date(c.date).toLocaleDateString()}</p>)}</section></div>}
function Card({title,value}){return <div className="card stat"><small>{title}</small><strong>{value}</strong></div>}

import React,{useEffect,useState}from'react';import{api}from'../services/api';
const empty={name:'',category:'ENERGY_LAYER',type:'ATAQUE',weight:'',material:'',advantages:'',disadvantages:'',compatibility:'',description:'',imageUrl:''};
export default function Pieces(){const[items,setItems]=useState([]),[f,setF]=useState(empty);const load=()=>api('/pieces').then(setItems);useEffect(()=>{load()},[]);async function save(e){e.preventDefault();await api('/pieces',{method:'POST',body:{...f,weight:f.weight?Number(f.weight):null}});setF(empty);load()}return <div><h2>Base de piezas</h2><form className="panel form-grid" onSubmit={save}>{Object.keys(empty).map(k=>k==='category'?<select key={k} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}><option value="ENERGY_LAYER">Layer</option><option value="FORGE_DISC">Disc</option><option value="PERFORMANCE_TIP">Driver</option><option value="CHIP_CORE">Chip/Core</option><option value="BLADE">Blade</option><option value="RATCHET">Ratchet</option><option value="BIT">Bit</option><option value="OTHER">Other</option></select>:<input key={k} placeholder={k} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>) }<button>Añadir pieza</button></form><div className="cards">{items.map(p=><article className="card" key={p.id}><span className="badge">{p.category}</span><h3>{p.name}</h3><p>{p.material} · {p.weight||'?'}g</p><p>{p.description}</p></article>)}</div></div>}
